import 'package:flutter/material.dart';

class UserModel {
  String userName, userEmail, userAddress, userPhoneNumber;
  UserModel(
      {@required this.userName,
      @required this.userEmail,
      @required this.userAddress,
      @required this.userPhoneNumber});
}
